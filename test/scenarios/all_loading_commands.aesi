{ ":load test/contracts/c.aes"
, {match, ok}
}.

{ ":lookup files"
, {match, ["test/contracts/c.aes"]}
}.

{ ":lookup includes"
, {match, ["test/contracts/c.aes"]}
}.

{ ":lookup files"
, {match, ["test/contracts/c.aes"]}
}.

{ ":lookup includes"
, {match, ["test/contracts/c.aes"]}
}.

{ "include \"test/contracts/d.aes\""
, {error, {repl_file_not_loaded, "test/contracts/d.aes"}}
}.

{ ":lookup includes"
, {match, ["test/contracts/c.aes"]}
}.

{ ":load test/contracts/d.aes test/contracts/c.aes"
, {match, ok}
}.

{ ":lookup includes"
, {match, ["test/contracts/d.aes"]}
}.

{ ":reload"
, {match, ok}
}.

{ ":lookup files"
, {match, ["test/contracts/d.aes", "test/contracts/c.aes"]}
}.

{ ":lookup includes"
, {match, ["test/contracts/d.aes"]}
}.
