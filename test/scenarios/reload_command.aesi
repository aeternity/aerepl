":load test/contracts/c.aes".
"let x = 1".
"datatype t = T".

{ ":lookup vars"
, { match
  , [ {"f", {fun_t, '_', [], [{id, '_', "int"}], {id, '_', "int"}}}
    , {"x", {id, '_', "int"}}
    ]
  }
}.

{ ":lookup types"
, {expect, ["t"]}
}.

{ ":lookup includes"
, {expect, ["test/contracts/c.aes"]}
}.

{ ":lookup files"
, {expect, ["test/contracts/c.aes"]}
}.

":reload".

{ ":lookup vars"
, {expect, []}
}.

{ ":lookup types"
, {expect, []}
}.

{ ":lookup includes"
, {expect, ["test/contracts/c.aes"]}
}.

{ ":lookup files"
, {expect, ["test/contracts/c.aes"]}
}.
