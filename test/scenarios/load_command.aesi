":load test/contracts/c.aes".

"let x = 1".

"datatype t = T".

"function f(x) = x + 1".

{ ":lookup vars"
, {match,
   [ {"f", {fun_t, '_', [], [{id, '_', "int"}], {id, '_', "int"}}}
   , {"x", {id, '_', "int"}}
   ]
  }
}.

{ ":lookup types"
, {match, ["t"]}
}.

{ ":lookup includes"
, {match, ["test/contracts/c.aes"]}
}.

":load test/contracts/d.aes".

{ ":lookup vars"
, {match, []}
}.

{ ":lookup types"
, {match, []}
}.

{ ":lookup includes"
, {match, ["test/contracts/d.aes"]}
}.
